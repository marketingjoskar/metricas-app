# ── Frontend (Vite) ──────────────────────────────────────────────────────────
# Estas variables se usan durante el BUILD y deben configurarse en Dokploy
VITE_SUPABASE_URL=https://XXXXXXXXXX.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key-aqui
VITE_DASHBOARD_PASSWORD=cambia-esta-password-2025

# ── Backend (Node.js / server.js) ────────────────────────────────────────────
# Estas variables se configuran en el panel de Dokploy → Environment Variables
# NO se necesitan en el .env del repositorio (son secretos del servidor)
DB_HOST=joskarerp.drogueriajoskar.com
DB_PORT=3939
DB_USER=indicadores
DB_PASSWORD=indica2026
DB_NAME=datasis

# Puerto donde escucha el servidor (Dokploy lo asigna automáticamente)
PORT=3001
